kind: Deployment
metadata:
    annotations:
        deployment.kubernetes.io/revision: "1"
    generation: 1
    labels:
        app: postgres
    name: postgres
spec:
    progressDeadlineSeconds: 600
    replicas: 1
    revisionHistoryLimit: 3
    selector:
        matchLabels:
            app: postgres
    strategy:
        type: Recreate
    template:
        metadata:
            creationTimestamp: null
            labels:
                app: postgres
                name: postgres
        spec:
            affinity:
                nodeAffinity:
                    requiredDuringSchedulingIgnoredDuringExecution:
                        nodeSelectorTerms:
                            - matchExpressions:
                                  - key: node-role.kubernetes.io/cloud-container-g2
                                    operator: In
                                    values:
                                        - "true"
            containers:
                - env:
                      - name: POSTGRES_USER
                        value: postgres
                      - name: POSTGRES_PASSWORD
                        value: postgres123
                      - name: POSTGRES_DB
                        value: voting
                  image: postgres:15-alpine
                  imagePullPolicy: IfNotPresent
                  name: postgres
                  ports:
                      - containerPort: 5432
                        name: http
                        protocol: TCP
                  resources:
                      limits:
                          cpu: 400m
                          ephemeral-storage: 1G
                          memory: 800M
                      requests:
                          cpu: 400m
                          ephemeral-storage: 1G
                          memory: 800M
                  terminationMessagePath: /dev/termination-log
                  terminationMessagePolicy: File
                  volumeMounts:
                      - mountPath: /mnt/postgres-disk-q2bcg/
                        name: postgres-disk-q2bcg
            dnsPolicy: ClusterFirst
            restartPolicy: Always
            schedulerName: default-scheduler
            securityContext: {}
            terminationGracePeriodSeconds: 30
            tolerations:
                - effect: NoSchedule
                  key: role
                  operator: Equal
                  value: cloud-container-g2
            volumes:
                - name: postgres-disk-q2bcg
                  persistentVolumeClaim:
                      claimName: postgres-disk-q2bcg
---
kind: PersistentVolumeClaim
metadata:
    annotations:
        pv.kubernetes.io/bind-completed: yes
        pv.kubernetes.io/bound-by-controller: yes
        volume.beta.kubernetes.io/storage-provisioner: cinder.csi.openstack.org
        volume.kubernetes.io/selected-node: caas-prd-worker-fr-10
        volume.kubernetes.io/storage-provisioner: cinder.csi.openstack.org
    finalizers:
        - kubernetes.io/pvc-protection
    name: postgres-disk-q2bcg
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 1G
    storageClassName: csi-cinder-sc-delete-az
    volumeMode: Filesystem
    volumeName: pvc-9dab0cc4-b865-4602-82ec-57d3dc990064
